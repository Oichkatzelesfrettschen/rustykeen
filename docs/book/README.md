# Docs book (mdBook)

This repo uses an mdBook-style structure for “Rust-native” narrative docs.

To build locally:
- Install mdBook: `cargo install mdbook`
- Run: `mdbook serve docs/book`

The book is complementary to the focused design/audit notes in `docs/`.

